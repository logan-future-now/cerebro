# Health dashboard assets (placeholder)

Cerebro currently serves a single-page UI from `index.html`.

If we later split JS/CSS into static assets, this directory can hold:
- health.css
- health.js
- chart helpers

For now, implementation is expected to stay inline in `index.html` per the architecture plan.
